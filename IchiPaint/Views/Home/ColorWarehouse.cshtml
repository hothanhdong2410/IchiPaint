@model IchiPaint.Models.ListColorWarehouse

@{
    ViewBag.Title = "Kho dữ liệu phối màu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="main_container">
    <div class="container" style="padding-bottom: 100px">
        <div class="max-830">
            <h2 class="color-main text-center" style="margin-bottom: 45px; margin-top: 45px;">
                <span>
                    Kho dữ liệu phối màu
                </span>
            </h2>
        </div>

        <div id="slideshow" class="single-slide owl-carousel s-dots s-nav s-auto s-loop">
            @foreach (var item in Model.Collection)
            {
                <img src="@item.Avatar" />
            }
        </div>
    </div>
</div>

@*<div id="main_container">
        <div class="container">
            <h2 class="color-main text-center" style="margin-bottom: 45px; margin-top: 45px;">
                <span> Kho dữ liệu phối màu </span>
            </h2>
        </div>

        <div class="max-830">
            @foreach (var item in Model.Collection)
            {
                <img  src="@item.Avatar" />
            }
        </div>
    </div>*@
